// var result = difference(5, 2);
// expect(result).to.eql(3);

//calculate intrust function

/**
*@param {number} principal Number of dollars barrowed
*@param {number} interest Interest rate, between 0 and 1
*@param {number} time Number of Year
*@return {number} The interest (not including the principal, obbviously)
**/

describe('calculateInterest()', function() {
  it('calculates interest of $10 barrowed at 4% for 1 year', function() {
    // principal
    // interest rate
    // amount of time
    // it returns inters
  });
  var interest = calculateInterest(10, 0.04, 1)

	});
//expect inerest of 0.41 and total amount of 10.41
		expect(interest).to,eql(0.41);
